'''
NumPy(기반)
└────── Pandas(데이터 처리)
        ├────── Matplotlib(기본 시각화)
        │           └────── Seaborn(통계 시각화)
        └────── KoNLPy(한국어 텍스트 분석)
                    └────── WorldCloud(텍스트 시각화)
Geopy(좌표 변환)
└────── Folium (지도 시각화)

NumPy → Pandas → Matplotlib → Seaborn → 나머지 이외 다양한 라이브러리

NumPy
- 수치 계산의 기초
- 다차원 배열 처리에 특화
- 행렬 연산, 선형 대수, 통계 함수 등 제공
- 거의 모든 데이터 분석 라이브러리의 기반

기본 Python으로 연산을 하는 것이 느려, C언어로 연산을 빠르게 만들어 파이썬 사용할 수 있도록 라이브러리 처리
데이터 분석을 할 때 NumPy를 직접적으로 쓸 일은 생각보다 적다.
Pandas 대부분 대신할 수 있다.

Pandas
- Excel / CSV 같은 테이블 데이터를 다루는 도구
- DataFrame(표), Series(열) 두 가지 자료구조 사용
- 데이터 필터링, 정렬, 집계, 결측값 처리 등

Matplotlib
-  파이썬 시각화의 가장 기본이 되는 라이브러리
-  꺾은선, 막대, 파이, 산점도 등 거의 모든 차트 기능
-  세부 꾸미기 기능이 많고 자유롭지만 코스가 많다...

Seaborn
- 예쁜 통계 시각화
- Matplotlib 기반 예쁜 기본 디자인
- 통계 그래프(히트맵, 분포도, 박스플롯)에 강함
- 코드가 짧고, PandasDataFrame 과 궁합 좋음

Folium
- 인터랙티브 지도 시각화
- 웹 기반 지도(Leaflet.js)를 파이썬으로 생성
- 지도 위에 마커, 히트맵, 폴리곤 등을 표시
- 결과물이 HTML 파일로 저장됨

WorldCloud
- 텍스트에서 단어 빈도를 분석해 시각화
- 자주 나오는 단어일수록 크게 표시
- 한국어 사용 시 KoNLPy와 함께 사용

KoNLPy
- 한국어 자연어 처리
- 한국어 텍스트를 형태소 단위로 분석
- Okt, Mecab, Kkma 등 다양한 형태소 분석기 내장
- 명사/동사/형용사 추출, 품사 태깅 가능

Geopy
- 주소 ←→ 좌표 변환
- 주소를 위도/경도 변환(지오 코딩)
- 두 지점 간 거리 계산
- Folium과 함께 쓰면 주소 기반 지도 시각화 가능

-- Geo라는 단어가 들어간다 --> 좌표 관련
'''