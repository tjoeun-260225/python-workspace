import matplotlib.pyplot as plt
import seaborn as sns
import pandas as pd
import numpy as np

plt.rcParams['font.family'] = 'D2Coding'
plt.rcParams['axes.unicode_minus'] = False

sns.set_theme(style='whitegrid')
sns.set_palette('pastel')

df = pd.read_csv('제주국제자유도시개발센터_JDC지정면세점_국적별_매출_정보_20241231.csv', encoding='euc-kr')


# =============================================
# TODO 1
# 컬럼 이름과 shape 를 출력하세요.
# =============================================
print(df.???)
print(df.???)


# =============================================
# TODO 2
# 국적 열에 어떤 값들이 있는지 확인하세요.
# 힌트 : value_counts()
# =============================================
print(df[???].???())


# =============================================
# TODO 3
# 국적별 매출 합계를 내림차순으로 출력하세요.
# 힌트 : groupby → sum → sort_values
# =============================================
국적별 = df.groupby(???)['매출'].???().sort_values(ascending=???)
print(국적별)


# =============================================
# TODO 4
# 국적별 매출 합계를 막대 그래프로 그리세요.
# 제목은 '국적별 연간 매출'
# =============================================
plt.???(국적별.index, 국적별.values)
plt.???(???)
plt.xticks(rotation=???)
plt.show()


# =============================================
# TODO 5
# 내국인 데이터만 필터링하고
# 월별 매출을 꺾은선 그래프로 그리세요.
# 제목은 '내국인 월별 매출'
# =============================================
내국인 = df[df[???] == ???]
plt.???(내국인['판매년월'], 내국인['매출'])
plt.???(???)
plt.xticks(rotation=45)
plt.show()


# =============================================
# TODO 6
# 내국인 제외하고 외국인 데이터만 필터링하세요.
# 힌트 : df[df[???] != ???]
# =============================================
외국인 = df[df[???] != ???]


# =============================================
# TODO 7
# 외국인 국적별 매출 합계를 구하고
# 가로 막대 그래프로 그리세요. (barh)
# 제목은 '외국인 국적별 매출'
# =============================================
외국인별 = 외국인.groupby(???)['매출'].sum().sort_values(ascending=???)
plt.???(외국인별.index, 외국인별.values)
plt.???(???)
plt.show()


# =============================================
# TODO 8
# 월별 전체 매출 합계를 구하고
# 가장 많이 팔린 달을 출력하세요.
# 힌트 : groupby → sum → idxmax()
# =============================================
월별 = df.groupby(???)['매출'].???()
print('매출 1위 월 :', 월별.???())


# =============================================
# TODO 9
# 국적별 매출 분포를 boxplot 으로 그리세요.
# x='국적', y='매출'
# 제목은 '국적별 매출 분포'
# =============================================
sns.???(x=???, y=???, data=df)
plt.???(???)
plt.xticks(rotation=45)
plt.show()


# =============================================
# TODO 10
# 1행 2열 서브플롯으로
# 왼쪽 = TODO 4 국적별 막대 그래프
# 오른쪽 = TODO 7 외국인 가로 막대 그래프
# =============================================
fig, axes = plt.subplots(???, ???, figsize=(14, 5))

axes[0].???(국적별.index, 국적별.values)
axes[0].set_title(???)
axes[0].tick_params(axis='x', rotation=45)

axes[1].???(외국인별.index, 외국인별.values)
axes[1].set_title(???)

plt.tight_layout()
plt.show()